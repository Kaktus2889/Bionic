# ADR 0004: Goal, Strategy and KPI foundation

Status: Accepted

Company goals are durable first-class records owned by agents. Strategy evaluation derives measurable progress from operational state instead of invented metrics. KPI measurements are append-only observations so analytics can later reconstruct trends. The initial metric is task completion; future strategy adapters can add revenue, customer and market KPIs without changing the goal lifecycle.
